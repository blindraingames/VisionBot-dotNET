namespace BlindRainGames.Utils.VisionBot
{
    public enum ERecognitionType
    {
        // Recognize images and text on it.
        all = 1,
        // Recognize only image.
        image = 2,
        // Recognize only text.
        text = 3
    }
}
//EndFile//